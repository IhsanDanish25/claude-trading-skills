[build]
builder = "dockerfile"

[deploy]
startCommand = "streamlit run examples/daily-market-dashboard/app.py --server.port=${PORT:-8501} --server.address=0.0.0.0 --server.headless=true"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
